import 'package:flutter/material.dart';

class Instellingen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dit is het instellingen'),
    );
  }
}
