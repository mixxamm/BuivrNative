import 'package:flutter/material.dart';

class Notificaties extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dit is het notificaties'),
    );
  }
}
