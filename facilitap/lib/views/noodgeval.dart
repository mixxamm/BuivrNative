import 'package:flutter/material.dart';

class Noodgeval extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dit is het noodgeval'),
    );
  }
}
