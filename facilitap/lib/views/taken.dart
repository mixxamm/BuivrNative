import 'package:flutter/material.dart';

class Taken extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dit is het taken'),
    );
  }
}
