import 'package:flutter/material.dart';

class Overzicht extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dit is het overzicht'),
    );
  }
}
